namespace M2.SapConnector;

/// <summary>
/// SAP NCo RFC/BAPI fallback interface (ADR-006).
/// Populated in a later sprint when OData gaps are identified.
/// </summary>
public interface ISapNcoClient
{
    // RFC/BAPI stubs added as gaps in OData coverage are discovered.
}
